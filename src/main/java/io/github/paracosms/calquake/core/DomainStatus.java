package io.github.paracosms.calquake.core;

/** Scientific-domain metadata retained outside the normal replay presentation. */
public enum DomainStatus {
    IN_DOMAIN,
    OUT_OF_DOMAIN,
    TRUNCATED
}
